# HeyGen Vercel Gateway

Restricted serverless relay for the news-avatar workbench. It only permits the HeyGen user check, video creation, and video status endpoints.

Required Vercel secret: `HEYGEN_API_KEY`.
